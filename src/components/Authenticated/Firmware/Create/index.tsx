import React from 'react'
import styles from './styles.module.scss'
const CreateFirmware: React.FC<any> = () => {
    return <div className={styles.container}>
            Create firmware
    </div>
}

export default CreateFirmware